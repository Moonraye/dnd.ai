# Postgres / Prisma
DATABASE_URL=

# Supabase (server-side JWT verification)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=

# AI Providers
GEMINI_API_KEY=

# App
PORT=3001
# Origin allowed for CORS (REST + WebSocket). Required when NODE_ENV=production
# (main.ts fails closed without it); local dev falls back to reflecting any origin.
CLIENT_URL=
